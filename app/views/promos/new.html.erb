<h1> Create a new Promo to post on your page</h1>

<%= form_for @event do |form| %>
  <div>
    <%= form.label :title %>
    <%= form.text_field :title, autofocus: true %>
  </div>

  <div class="form-group">
    <%= form.label :description%>
    <%= form.text_field :description %>
  </div>

  <div class="form-group">
   #drop down box
  </div>

  <button type="submit">
    Add event
  </button>
<% end %>
